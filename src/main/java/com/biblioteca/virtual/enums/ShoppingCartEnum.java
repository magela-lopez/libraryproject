package com.biblioteca.virtual.enums;

public enum ShoppingCartEnum {

    //"ACTIVE", "REMOVED", "EMPTY", "EXPIRED", "AWAITING PAYMENT", "PAY OUT")

    ACTIVE,
    REMOVED,
    EMPTY,
    EXPIRED,
    AWAITING_PAYMENT,
    PAY_OUT;
}
